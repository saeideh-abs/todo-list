export const Header = () => {
  return (
    <div
      className={`w-full h-header bg-white fixed px-5 flex items-center justify-center
      shadow-header text-lg font-semibold`}
    >
      Todo List
    </div>
  )
}
